# oracle config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springsecurity
    username: ohgiraffers
    password: ohgiraffers

# mybatis mapper.xml add
mybatis:
  mapper-locations: mappers/**/*.xml